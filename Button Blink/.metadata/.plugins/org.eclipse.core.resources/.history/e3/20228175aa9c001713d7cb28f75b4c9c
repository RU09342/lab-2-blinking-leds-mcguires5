#include <msp430.h> 


/**
 * main.c
 */
int main(void)
{
	WDTCTL = WDTPW | WDTHOLD;	// stop watchdog timer
	P1DIR |= (0x01);
	P2DIR &= (BIT1);
	P2IN1 &= (0x00000000);
	while(1){
	    if(P2IN1 == (BIT1)){
	        P1OUT ^= (0x01);
	    }
	}
	return 0;
}
